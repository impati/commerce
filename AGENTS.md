# AGENTS.md

Impati Commerce에서 Codex가 따라야 하는 저장소 지침이다. 이 파일은 작업 방법의 진입점이고,
서비스 구성과 포트는 [README.md](README.md), 시스템 경계는
[docs/architecture.md](docs/architecture.md)와 [docs/domain-map.md](docs/domain-map.md), 이미 내린
결정은 [docs/adr/](docs/adr/)를 정본으로 삼는다. 작업을 이어서 시작할 때는
[docs/backlog/README.md](docs/backlog/README.md)에서 현재 우선순위와 남은 항목을 먼저 확인한다.

## 설계 기준

- 현재 코드가 미완성인 사실을 설계 근거로 쓰지 않는다. 도메인·응용 계층과 포트는 실제 운영
  수준으로 완성하고, 실제 외부 업체와 운영 인프라만 교체 가능한 로컬 구현으로 대신한다.
- 외부 업체를 붙이지 않는다는 범위가 장애·재시도·데이터 수렴까지 생략한다는 뜻은 아니다.
  핵심 로직은 어떤 어댑터가 선택됐는지 몰라야 하며 실제 구현으로 바꿔도 수정되지 않아야 한다.
- 옳은 경계를 먼저 정하고 작업량이 크면 백로그를 나눈다. 변경량을 줄이려고 책임을 잘못된
  계층으로 옮기지 않는다.
- 예방적 구조를 상상만으로 추가하지 않는다. 현재 호출 경로에서 문제가 성립하는지 확인하고,
  아직 없는 기능이 전제라면 그 기능의 백로그에서 함께 판단한다.

## 인터뷰와 구현 전 시스템 경계 검토

서비스·프로세스·DB·브로커·외부 업체 경계를 넘는 작업은 구현 전에 다음 관점을 적용한다.
항목을 기계적으로 모두 묻는 체크리스트가 아니라, 해당 위험이 있는지 코드와 계약에서 확인하고
결정이 필요한 것만 사용자와 합의한다.

- 정본과 책임: 어느 시스템이 사실과 불변식을 소유하며, 다른 시스템은 무엇을 투영하는가.
- 커밋 경계: 무엇이 함께 내구화되어야 하고 로컬 트랜잭션, Outbox, Saga 중 무엇이 보장하는가.
- 결과 불명: 요청 성공 여부를 응답만으로 확정할 수 없을 때 어떤 식별자로 조회·재시도·대사하여
  최종 상태에 수렴하는가.
- 멱등성과 경합: 멱등 키를 누가 만들고 계약이 어떤 중복 결과를 보장하며, 동시 명령과 역순 사건의
  승자를 DB 제약·잠금·버전·조건부 갱신 중 무엇으로 정하는가.
- 실패 전파: 타임아웃, 부분 실패, 프로세스 종료, 재시작 뒤에도 의도와 복구 정보가 남는가.
- 검증 의무: 위 보장을 깨뜨릴 수 있는 실제 실패 시나리오를 어떤 자동화 테스트가 증명하는가.

인터뷰 완료 브리프에는 적용된 관점과 결정을 포함한다. 제품 정책이나 책임 소유권처럼 결과가
달라지는 질문은 사용자에게 묻고, 코드와 기존 결정에서 알 수 있는 기술 사실은 먼저 조사한다.
리뷰에서는 구현이 합의를 지켰는지뿐 아니라 인터뷰가 필요한 경계를 빠뜨리지 않았는지도 다시
확인한다.

## 외부 연동의 책임 경계

출력 포트는 애플리케이션이 필요한 능력과 보장을 선언하고 `adapter/out` 구현이 이를 수행한다.

- 어댑터는 업체별 인증·서명, 전송, 직렬화, 상태 및 오류 코드 변환을 소유한다.
- 애플리케이션은 업무 멱등 키, 내구화된 처리 의도, 결과 불명 상태, 재시도·조회·대사, 로컬 상태
  수렴과 트랜잭션 순서를 소유한다.
- 외부 시스템에 변경을 요청할 때는 의도를 먼저 로컬 DB에 커밋하고 DB 트랜잭션 밖에서 호출한 뒤,
  결과와 로컬 상태를 하나의 로컬 트랜잭션으로 확정한다. 네트워크 호출을 연 DB 트랜잭션 안에서
  기다리지 않는다.
- 호출자가 만든 안정적인 업무 멱등 키와 결과 확인 방법을 포트 계약에 드러낸다. 응답 유실은 실패로
  단정하지 않고 `결과 불명`으로 다루며, 재시작 뒤에도 같은 키로 복구할 수 있어야 한다.
- 업체가 멱등성이나 조회 가능성을 보장한다고 위임할 때도 그 사실을 구현의 암묵적 가정으로 두지
  않는다. 포트 계약에 명시하고 로컬 어댑터와 계약 테스트가 같은 의미를 보장하게 한다.
- 로컬 대역 구현만 자신이 대역임을 안다. 도메인·응용 계층과 인바운드 어댑터가 프로파일, 업체명,
  테스트 여부로 분기하지 않는다.

비동기 사건은 적어도 한 번 전달될 수 있다고 가정한다. 발신 상태와 Outbox를 함께 커밋하고,
수신자는 사건 식별자나 합의된 업무 키로 중복을 제거한다. 역순·지연·서로 모순된 종결 사건이
현재 상태를 후퇴시키거나 덮지 않게 하며, 정본과 투영이 일시적으로 달라도 자동 재시도나 대사로
수렴해야 한다. 조회 후 저장하는 코드만으로 동시성을 보장하지 말고 최종 승자를 내구 저장소에서
원자적으로 정한다.

## 테스트가 완료를 정의한다

커버리지 비율을 목표로 삼지 않는다. 변경이 약속한 불변식과 실제 실패 양상을 자동화 테스트와
일대일로 연결하고, 테스트가 없으면 그 보장은 구현되지 않은 것으로 본다.

- 도메인·응용 로직은 정상 경로와 거절·보상 경로를 함께 검증한다.
- 원격 변경은 외부 성공 뒤 응답 유실, 결과 저장 전 종료, 재시작 후 재시도, 같은 키의 동시 요청을
  검증한다.
- 비동기 처리는 중복 전달, 역순 전달, 재시도, 동시 소비와 정본·투영 수렴을 검증한다.
- 상태 전이는 동시에 실행되는 상충 명령에서 한 결과만 확정되는지 검증한다.
- 새로 만들거나 크게 바꾼 핵심 테스트는 검증 대상 로직을 의도적으로 깨뜨려 실패하는지 확인한 뒤
  원복한다. 통과만 하는 테스트를 완료 근거로 삼지 않는다.

작업 완료 전 `make verify`를 실행한다. 프론트엔드를 건드렸다면 `make frontend-test`와
`make frontend-build`도 실행하고, 모든 변경에서 `git diff --check`를 확인한다. 실행하지 못했거나
실패한 검증은 성공처럼 요약하지 말고 그대로 보고한다.

## 작업과 이력

- 기능 동작에 영향을 주는 작업은 `BL-NNNN`에서 시작하고
  `feature/bl-NNNN-kebab-case-title` 브랜치와 `<type>(bl-NNNN): 한국어 요약` 커밋으로 잇는다.
- 정책은 `docs/policy/`, 기술 결정과 기각한 대안은 `docs/adr/`, 미래 작업은 `docs/backlog/`에 둔다.
  구현 상세와 진행 일지는 커밋이 정본이다.
- 구현과 검증이 끝나도 백로그를 바로 `done/`으로 옮기지 않는다. `rca-code-review` 완료 판정과 사용자
  승인 뒤에 이동한다.
- 작업을 마치면 의도한 파일만 스테이징해 리뷰 가능한 단위로 커밋한다. 푸시와 PR은 요청받았을 때만
  수행한다.
- `.claude/settings.local.json`은 개인 설정이므로 읽거나 수정하거나 커밋하지 않는다.
