# 변경 기록

최신 변경부터 기록한다. 각 항목은 변경 의도와 핵심 내용, 중요한 결정만 간결하게 남긴다. **파일별 변경 목록이나 완료한 일의 체크리스트는 쓰지 않는다** — 그건 `git log`가 정본이다. 개별 백로그 항목 추가는 기록 대상이 아니다.

이 저장소는 버전을 매기지 않으므로 날짜와 제목으로 구분한다.

## 2026-08-16 — 문서 체계를 플러그인 규약에 맞췄다

- **의도:** `impati-agent-tools` 플러그인이 매 세션 주입하는 공통 규칙과 이 저장소의 `CLAUDE.md`가 개념·이름 수준에서 어긋나 있었다. 충돌 시 플러그인을 따르기로 정하고 양쪽을 한 체계로 합쳤다.
- **변경:** `NEXT.md`를 없애고 `docs/backlog/`로 옮겼다 — 항목당 한 파일에 `BL-NNNN` 추적 ID를 부여하고(16건), 우선순위는 `docs/backlog/README.md`가 갖는다. `decisions/`를 `docs/adr/`로 옮겨 ADR 번호·상태 형식으로 맞췄다. `CHANGELOG.md`를 도입했다. `CLAUDE.md`에 작업 흐름(`capture-backlog` → `start-work` → ADR → `rca-code-review`)과 커밋·브랜치 컨벤션을 넣었다.
- **결정:** 커밋 룰을 "사용자가 요청할 때만"에서 "작업이 끝나면 리뷰 단위로 커밋하고 보고"로 뒤집었다. 푸시와 PR은 여전히 요청받았을 때만 한다.
- **결정:** `CHANGELOG.md`는 변경 의도만 담는다. "완료된 일 목록을 문서로 만들지 않는다"는 기존 룰을 폐기하지 않고 이 파일의 형식 제약으로 남겼다 — `git log`와 두 벌이 되는 것이 원래 우려였다.
- **유지:** `problem/`은 플러그인과 충돌이 없어 그대로 둔다. 사용자가 요구할 때만 쓴다는 규칙도 유지한다.
