# 이어서 할 일

작업을 끊고 다시 잡을 때 여기서 시작한다. **완료된 일은 적지 않는다** — 그건 `git log`가 정본이고, 두 벌이 되면 반드시 어긋난다.

## 이 문서가 담는 것과 담지 않는 것

| | 어디 | 왜 |
| --- | --- | --- |
| 코드에 붙는 부채 | 코드의 `TODO` 주석 | 코드와 어긋날 수 없다. 코드를 지우면 부채도 사라진다 |
| 우선순위·방향·합의 | **이 문서** | 코드 주석에 담기지 않는다 |
| 기술 결정과 근거 | `decisions/` | 무엇을 왜 골랐나. 재검토 조건까지 |
| 겪은 문제와 조사 | `problem/` | 사용자가 요청할 때만 쓴다 |
| 무엇을 왜 했나 | `git log` | 커밋 메시지에 근거를 남긴다 |

코드에 남은 부채의 정본은 이 문서가 아니라 grep이다.

```bash
make todo
```

## 지금 우선순위

### 1. 게이트웨이 우회 차단

서비스가 8101~8109로 직접 노출돼 있다. 위험이 둘이다.

**쓰기 방향** — 게이트웨이를 건너뛰고 `X-Member-Id`를 위조하면 아무 회원으로 행세할 수 있다.

**읽기 방향** — 게이트웨이가 TLS를 끝내면 그 뒤는 평문 HTTP다. 이 구간에 접근할 수 있으면 `Authorization` 헤더와 `X-Member-Id`를 그대로 읽는다. 암호화가 애초에 없는 구간이므로 토큰 탈취 경로 중 가장 현실적이다.

인증을 붙여놓고 뒷문이 열려 있는 상태이므로 다른 어떤 작업보다 먼저다.

로컬에서 표현할 수 있는 방법은 서비스 간 공유 시크릿 헤더 검증이다. 게이트웨이가 붙이고 서비스가 확인한다. compose 네트워크에서 포트 노출을 제거하는 것은 로컬 프로세스 실행에서는 표현되지 않는다. 운영이라면 내부 구간도 mTLS로 암호화하는 것이 답이다.

관련: [MemberIdentity](apps/api-gateway/src/main/java/com/impati/commerce/gateway/support/MemberIdentity.java), [decisions/001](decisions/001-session-token-strategy.md)

### 2. 인증 링크 토큰의 Referer 유출

인증 링크가 `/verify?token=...` 형태로 토큰을 URL에 담는다. 그 페이지가 외부 호스트에 요청을 보내면 `Referer` 헤더에 토큰이 실려 나간다.

프론트가 `images.unsplash.com`에서 상품 이미지를 로드하므로 이 경로가 실재한다. 인증 처리 후 `history.replaceState`로 URL을 지우지만 이미지 로드가 그보다 먼저 시작되므로 순서상 막지 못한다.

세션 토큰은 헤더로만 보내므로 이 문제가 없다. URL에 토큰이 실리는 곳은 인증 링크 하나다.

대응 후보는 둘이다.

| 방법 | 효과 |
| --- | --- |
| `Referrer-Policy: no-referrer` | 브라우저가 Referer를 보내지 않는다. 한 줄이고 즉시 효과가 있다 |
| 토큰을 URL 프래그먼트(`#token=`)로 옮긴다 | 프래그먼트는 서버로 전송되지 않고 Referer에도 실리지 않는다. 더 근본적이지만 링크 형태와 프론트 파싱이 바뀐다 |

먼저 헤더로 막고, 프래그먼트 전환은 별도로 판단한다.

### 3. 세션 확인의 가용성 결합과 지연

불투명 토큰이라 게이트웨이가 요청마다 member-service를 부른다. 폐기 즉시성을 얻은 대가인데 문제가 둘이고 무게가 다르다.

**가용성 결합이 더 무겁다.** member-service가 죽으면 전 서비스의 인증이 죽는다. 상품 조회는 되는데 로그인한 사용자의 장바구니·주문은 전부 실패한다. 캐시를 붙여도 미스 시에는 여전히 의존하므로 이 결합은 남는다.

**지연은 캐시로 줄인다.** 짧은 TTL 캐시를 붙이면 되고, 그 TTL이 폐기 지연 상한이 된다는 점을 정해야 한다.

결합까지 끊으려면 하이브리드로 가야 한다 — 짧은 수명(5~15분)의 서명 토큰으로 요청당 조회를 없애고, 폐기 즉시성이 필요한 refresh 쪽만 상태를 갖는다. 그러면 폐기가 "즉시"에서 "access token 수명 안에"로 바뀐다. 그 거래를 받아들일지가 결정 사항이다.

JWT를 단독으로 쓰지 않는 이유는 폐기 때문이다. 블랙리스트를 매 요청 조회하면 불투명 토큰과 같아져 이점이 사라진다.

### 4. member-service 아웃박스

인증 메일 요청이 가입 트랜잭션 안에 있다. 실패해도 가입은 유지되지만 상대가 느리면 DB 트랜잭션이 함께 늘어난다. notification-service에 만든 아웃박스와 같은 형태가 필요하다.

order-service → notification-service 구간도 같은 문제다. 그쪽은 예외를 삼켜 실패가 기록되지 않는다.

### 5. 프론트 토큰 보관을 쿠키로

`localStorage`는 XSS로 읽힌다. HttpOnly 쿠키가 맞고, 그러면 게이트웨이가 쿠키를 세팅하고 CSRF 대응이 따라온다.

## 합의된 방향 (재논의하지 않는다)

이미 판단이 끝난 것들이다. 뒤집으려면 새 근거가 필요하다.

| 주제 | 결정 |
| --- | --- |
| 이메일 유일성 | 대소문자를 구분한다. 중복 계정 위험은 소유 인증으로 막는다 |
| 세션 토큰 | 불투명 토큰. 근거와 재검토 조건은 [decisions/001](decisions/001-session-token-strategy.md) |
| 메일 발송 소유 | notification-service. 벤더 지식이 한 서비스에만 모인다 |
| 퍼블릭 API | `memberId`를 받지 않는다. 신원은 세션에서만 온다 |
| 테스트 상한 | `@SpringBootTest` + HTTP stub. 프로세스를 띄우는 e2e는 만들지 않는다 |
| 인프라 | 실제 외부 호출과 실 DB는 붙이지 않는다. 어댑터만 갈아끼우면 나갈 수 있는 상태가 목표 |

## 미결정 (정해야 할 때 정한다)

- `Money`를 도메인별로 분리하기로 합의했으나 아직 하지 않았다. 지금은 `ApiContracts.Money`를 order/payment/catalog 도메인이 함께 쓴다
- `Ids.newId`가 UUID를 12자(48비트)로 잘라 쓴다. 약 1,700만 건에서 충돌 확률 50%이고 시간 정렬이 안 된다. PK라 미루면 비싸진다
- 테스트 격리가 작성자 규율에 의존한다. `@Transactional` 롤백으로 바꿀지 미정
- `docker-compose.yml` 경로는 한 번도 실행해보지 않았다
- 마이그레이션을 파일 DB에 적용하는 검증이 하네스에 없다. `./gradlew test`는 매번 빈 in-memory DB에서 돌려 체크섬 충돌을 잡지 못한다

## 알려진 한계 (고칠 계획 없음)

- pre-commit 훅은 working tree를 검사한다. 부분 커밋 시 커밋되지 않는 변경까지 포함해 검증한다
- 지면 카드 가격이 첫 SKU 가격이다. 변형별 가격이 다르면 표시 가격과 실제 가격이 어긋난다
