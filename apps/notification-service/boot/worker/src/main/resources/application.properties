spring.config.import=classpath:application-shared.properties

spring.application.name=notification-worker
server.port=8119

# 발송기가 도는 주기. @Scheduled가 밀리초만 받으므로 공용 설정의 retry-delay와 단위가 다르다.
notifications.dispatch-interval=1000
