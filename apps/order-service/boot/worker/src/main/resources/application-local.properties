# 로컬 브로커는 노드가 하나라 운영 값(복제 3 · 최소 ISR 2)으로는 토픽을 만들 수 없다.
#
# 내리는 것을 여기에만 두는 이유는 기본값이 운영 값이어야 하기 때문이다 (ADR-0016). 여기서
# 확인되는 것은 흐름이고, 복제가 실제로 유실을 막는지는 아니다.
commerce.kafka.order-events-replicas=1
commerce.kafka.order-events-min-insync-replicas=1
