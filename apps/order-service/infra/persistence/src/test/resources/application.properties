# core의 통합 테스트가 쓰는 설정. 공용 설정을 그대로 가져온다.
#
# 실행 단위(api·worker)가 하는 것과 같은 방식으로 가져오므로, 공용 설정이 실제로 읽히는지도
# 여기서 함께 확인된다.
spring.config.import=classpath:application-shared.properties

spring.application.name=order-core-test
