# 프론트 토큰 보관을 쿠키로

- **ID:** BL-0005
- **기록일:** 2026-08-16

## 배경

프론트가 세션 토큰을 `localStorage`에 보관한다. `localStorage`는 XSS로 읽힌다.

## 목표

세션 토큰이 스크립트로 읽히지 않는다. HttpOnly 쿠키가 맞고, 그러면 게이트웨이가 쿠키를 세팅하고 CSRF 대응이 따라온다.
